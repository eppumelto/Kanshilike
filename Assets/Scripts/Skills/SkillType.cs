/// <summary>
/// All trainable skills. Add new entries here to extend the system —
/// then create a matching SkillDefinition asset in Assets/Skills/Data/.
/// </summary>
public enum SkillType
{
    Unarmed,
    Axes,
    Strength,
    Athletics,
    Harvesting,
    Dexterity,
    Block,
}
